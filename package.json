{
  "name": "n8n-monorepo",
  "version": "1.39.1",
  "private": true,
  "engines": {
    "node": ">=18.10",
    "pnpm": ">=8.12.0"
  },
  "packageManager": "pnpm@8.15.4",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "turbo run format"
  }
}
